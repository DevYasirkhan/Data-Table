{
  "name": "cls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-json": "json-server --watch data.json --port 3000"
  },
  "devDependencies": {
    "sass": "^1.85.1",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
