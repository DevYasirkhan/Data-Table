{
  "name": "cls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "concurrently \"npm run serve-json\" \"vite\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-json": "json-server --watch data.json --port 3000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "sass": "^1.85.1",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "zod": "^3.24.2"
  }
}
